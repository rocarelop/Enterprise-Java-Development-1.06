# Enterprise-Java-Development-1.06
1. En este ejercicio he creado la clase `Employee` con sus atributos, método constructor y setters y getters. 
2. Tras ello he creado la clase `Intern` que hereda de la clase `Employee` pero a la que añado un nuevo atributo constante. En esta clase además añado una condición para que el atributo salario no sobrepase la cantidad indicada.
3. Para finalizar en la clase `Main` creamos un documento.txt donde recogemos todos los empleados.
